---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const valuesEntries = await getCollection("values");
const values = valuesEntries.sort((a, b) => a.data.order - b.data.order);

const reasons = [
  {
    title: "Impacto real",
    description: "Tu trabajo transforma directamente la transparencia y sostenibilidad de toda una industria.",
  },
  {
    title: "Desde el inicio",
    description: "Únete en la etapa fundacional. Moldea el producto, la cultura y el futuro de TTL.",
  },
  {
    title: "Flexibilidad total",
    description: "Trabajo remoto, horarios flexibles y colaboración asíncrona desde cualquier lugar de Latinoamérica.",
  },
];
---

<Layout title="Empleo">
  <div class="isolate">
    <!-- Header -->
    <div class="px-6 pt-8 pb-16 lg:px-8">
      <div class="mx-auto max-w-2xl pt-12 text-center sm:pt-20">
        <h1 class="text-5xl font-semibold tracking-tight text-neutral-900 sm:text-7xl dark:text-white">Trabaja con nosotros</h1>
        <p class="mt-8 text-lg font-medium text-pretty text-neutral-500 sm:text-xl/8 dark:text-neutral-400">Únete a un equipo que está transformando la trazabilidad textil en América Latina. Buscamos personas apasionadas por la sostenibilidad y la innovación.</p>
      </div>
    </div>

    <!-- ¿Por qué unirte? -->
    <div class="bg-white py-16 sm:py-20 dark:bg-neutral-900">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl lg:text-center">
          <p class="text-base/7 font-semibold text-primary-600 dark:text-primary-400">Oportunidad única</p>
          <h2 class="mt-2 text-4xl font-semibold tracking-tight text-pretty text-neutral-900 sm:text-5xl dark:text-white">¿Por qué unirte?</h2>
        </div>
        <div class="mx-auto mt-16 max-w-2xl sm:mt-20 lg:mt-24 lg:max-w-none">
          <dl class="grid max-w-xl grid-cols-1 gap-8 lg:max-w-none lg:grid-cols-3">
            {reasons.map((reason) => (
              <div class="flex flex-col rounded-2xl border border-primary-100 bg-white p-8 dark:border-neutral-700 dark:bg-neutral-800">
                <dt class="text-base/7 font-semibold text-neutral-900 dark:text-white">
                  {reason.title}
                </dt>
                <dd class="mt-4 flex flex-auto flex-col text-base/7 text-neutral-600 dark:text-neutral-400">
                  <p class="flex-auto">{reason.description}</p>
                </dd>
              </div>
            ))}
          </dl>
        </div>
      </div>
    </div>

    <!-- Lo que nos mueve -->
    <div class="bg-neutral-50 py-16 sm:py-20 dark:bg-neutral-900">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl lg:text-center">
          <p class="text-base/7 font-semibold text-primary-600 dark:text-primary-400">Nuestros valores</p>
          <h2 class="mt-2 text-4xl font-semibold tracking-tight text-pretty text-neutral-900 sm:text-5xl dark:text-white">Lo que nos mueve</h2>
        </div>
        <div class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-8 sm:grid-cols-2 lg:max-w-none lg:grid-cols-3">
          {values.map((value) => (
            <div class="border-l-2 border-primary-200 pl-6 dark:border-primary-700">
              <h3 class="text-base/7 font-semibold text-neutral-900 dark:text-white">{value.data.title}</h3>
              <p class="mt-2 text-base/7 text-neutral-600 dark:text-neutral-400">{value.body}</p>
            </div>
          ))}
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="bg-white py-16 sm:py-20 dark:bg-neutral-900">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl text-center">
          <h2 class="text-4xl font-semibold tracking-tight text-pretty text-neutral-900 sm:text-5xl dark:text-white">¿Listo para unirte?</h2>
          <p class="mx-auto mt-6 max-w-xl text-lg/8 text-neutral-600 dark:text-neutral-400">No importa si no hay vacantes abiertas. Si compartes nuestra visión, queremos conocerte.</p>
          <div class="mt-10 flex items-center justify-center gap-x-6">
            <a href="mailto:info@textiletrace.lat" class="rounded-md bg-primary-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-primary-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600 transition-colors">Escríbenos</a>
            <a href="/nosotros" class="text-sm/6 font-semibold text-neutral-900 dark:text-white">Conócenos <span aria-hidden="true">&rarr;</span></a>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
